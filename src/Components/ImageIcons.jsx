const ImageIcons =({icons, onClick})=>{


    return (        
    
            <div onClick={onClick} className="cursor-pointer">
                {icons}
            </div>
    
    )


}
export default ImageIcons 